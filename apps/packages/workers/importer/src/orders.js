// IPRoyal order management stubs (to be extended in API or worker):
// Example: sync orders
// axios.get(`${p.config.base_url || 'https://apid.iproyal.com'}/v1/reseller/orders`, { headers: { 'X-Access-Token': p.config.access_token } })
